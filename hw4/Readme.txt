This program is completed in the development environment of Pycharm. 
In order to prevent the file from being too large, I deleted the library folder, so you may need to install package numpy, install package opencv-python, install package cv2, install package matplotlib before executing it.

Just press execute in 113598045_hw4.py to generate 9 result images and 3 videos.
The number of iterations and DSC value will be displayed on the terminal below.
